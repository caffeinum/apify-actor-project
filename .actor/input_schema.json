{
  "title": "Actor Builder Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "prompt": {
      "title": "Prompt",
      "type": "string",
      "description": "Describe the actor you want to build. Be specific about what it should do, what inputs it needs, and what outputs it should produce.",
      "editor": "textarea",
      "prefill": "Create a simple web scraper actor that takes a URL as input and extracts all links from the page"
    },
    "actorName": {
      "title": "Actor Name",
      "type": "string",
      "description": "Name for the new actor (lowercase, no spaces, use hyphens)",
      "editor": "textfield",
      "prefill": "my-new-actor",
      "pattern": "^[a-z0-9-]+$"
    }
  },
  "required": ["prompt", "actorName"]
}
